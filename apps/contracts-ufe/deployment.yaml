apiVersion: apps/v1
kind: Deployment
metadata:
  name: contracts-ufe-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
        pod: contracts-ufe-label
  template:
    metadata:
      labels:
        pod: contracts-ufe-label
    spec:
      containers:
      - name: contracts-ufe-container
        image: thepikart/contracts-ufe
        imagePullPolicy: Always

        ports:
        - name: http
          containerPort: 8080

        resources:
            requests:
                memory: "32M"
                cpu: "0.1"
            limits:
                memory: "320M"
                cpu: "0.3"