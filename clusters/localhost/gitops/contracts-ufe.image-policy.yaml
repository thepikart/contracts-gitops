apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: contracts-ufe
  namespace: wac-hospital
spec:
  imageRepositoryRef:
    name: contracts-ufe 
  filterTags:
      pattern: "main.*"
  policy:
    alphabetical:
      order: asc